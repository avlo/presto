plugins {
    id 'buildlogic.java-conventions'
}

dependencies {
    api project(':lib-core')
    api project(':lib-web')
    api project(':lib-ldap')
}

description = 'autoconfigure-ldap'

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = description
            from(components.java)
        }
    }
}

bootJar {
    enabled = false
}
